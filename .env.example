# Google Analytics 4 measurement ID (public, safe to expose).
# Leave empty to disable analytics (e.g. in local development).
NEXT_PUBLIC_GA_ID=

# Future (not used yet) — server-only secrets must NOT use the NEXT_PUBLIC_ prefix.
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
